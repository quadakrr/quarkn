# Quarkn
Simple Linux CLI notification tool.

# Features
- Timed notifications
- Interactive mode
- Optional sound playback
- Command execution

# Requirements
- Linux
- notify-send
- mpv / ffplay / vlc (optional)

# Example
```bash
python3 quarkn.py -t 10m -m 'Take a break'
